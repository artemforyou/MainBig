<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Main Big</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>
        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            background: linear-gradient(135deg, #0b0f1a, #1a1f2e);
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            color: white;
        }

        .container {
            text-align: center;
            background: rgba(255, 255, 255, 0.05);
            padding: 50px 60px;
            border-radius: 14px;
            box-shadow: 0 0 40px rgba(0,0,0,0.7);
        }

        h1 {
            font-size: 56px;
            margin-bottom: 10px;
            letter-spacing: 2px;
        }

        p {
            font-size: 18px;
            opacity: 0.8;
            margin-bottom: 35px;
        }

        button {
            background: #4da3ff;
            border: none;
            border-radius: 8px;
            padding: 14px 32px;
            font-size: 18px;
            cursor: pointer;
            color: black;
            transition: 0.2s;
        }

        button:hover {
            background: #78b9ff;
            transform: scale(1.05);
        }
    </style>
</head>
<body>

<div class="container">
    <h1>Main Big</h1>
    <p>Игровая платформа в разработке</p>

    <button onclick="alert('Здесь будет контент Main Big')">
        Перейти
    </button>
</div>

</body>
</html>